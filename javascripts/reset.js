"use strict";
var AUTOMOBILES = (function (autos) {

autos.resetBorderBackground = function () {
	//reset border  thickness and background color to default
};

autos.changeStyle = function(thickness, backgroundColor) {//two args: thick, bgcolr
	//change border thickeness
	//change car background color
};

	return autos

})(AUTOMOBILES || {})

// console.log(AUTOMOBILES.populate(inventory));